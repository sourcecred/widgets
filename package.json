{
  "name": "@sourcecred/widgets",
  "version": "0.0.1-alpha",
  "private": true,
  "dependencies": {
    "esm": "^3.2.25",
    "got": "^9.6.0",
    "keyv-fs-sync": "^0.1.0",
    "mkdirp": "^0.5.1",
    "sharp": "^0.23.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.2",
    "@babel/core": "^7.6.2",
    "@babel/preset-env": "^7.6.2",
    "@babel/preset-flow": "^7.0.0",
    "jest": "^24.9.0",
    "prettier": "^1.18.2",
    "rimraf": "^3.0.0"
  },
  "scripts": {
    "build": "yarn -s build:clean && yarn -s build:lib",
    "build:clean": "rimraf lib",
    "build:lib": "babel -d lib src",
    "prettify": "prettier --write '{src,test}/**/*.js'",
    "check-pretty": "prettier --list-different '{src,test}/**/*.js'",
    "test": "yarn run unit && yarn run --silent check-pretty",
    "unit": "jest"
  },
  "license": "(MIT OR Apache-2.0)"
}
